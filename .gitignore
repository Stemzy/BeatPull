# Build outputs (huge - never commit these)
build/
dist/
*.zip

# Python caches
__pycache__/
*.pyc

# Big binaries kept locally for building, not in git
ffmpeg.exe
ffprobe.exe

# Local app data / user library (never commit)
.beatpull/

# OS junk
Thumbs.db
.DS_Store
desktop.ini
